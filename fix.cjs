const mysql = require('mysql2/promise'); require('dotenv').config(); async function run() { const pool = mysql.createPool({ host: process.env.DB_HOST, user: process.env.DB_USER, password: process.env.DB_PASSWORD, database: process.env.DB_NAME }); const [rows] = await pool.query('SELECT id, titulo, status FROM gabarito_avaliacoes WHERE status = \'rascunho\' AND gabarito_oficial IS NOT NULL'); console.log(rows); await pool.query('UPDATE gabarito_avaliacoes SET status = \'publicada\' WHERE status = \'rascunho\' AND gabarito_oficial IS NOT NULL'); process.exit(0); } run();
